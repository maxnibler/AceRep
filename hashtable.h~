#include<iostream>
#include<cstdlib>
#include<string>
#include<cstdio>
using namespace std;

const int tableSize;
class HashTableItem{
 public:
  int x;
  int y;
  HashTableItem(int x, int y){
    this -> x = x;
    this -> y = y;
  }
};
