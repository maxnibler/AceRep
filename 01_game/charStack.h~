class charStack{
 private:
  int top;
  int size;
  char base;
  char data[10];
 public:
  char peek();
  char pop();
  void push(char c);
  void setBase(char c);
  charStack(){
    top = -1;
    size = 0;
    base = '.';
  }
};
